# Bazel
bazel-*
.bazel-cache/

# jiradozer runtime artifacts
.jiradozer/

# Claude Code per-project state
.claude/scheduled_tasks.lock

# code-review eval dataset / replay results — derived from private PRs,
# must never be committed. Canonical home is ~/.bramble/code-review-eval/;
# this guards against an --out-dir that points back inside the repo.
.claude/skills/code-review-eval/data/dataset/
.claude/skills/code-review-eval/data/replays/

# Python
__pycache__/
*.pyc

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.work.sum

# IDEs
.idea/
.vscode/
.cursor/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

