# Review Eval Run History

Append new eval results below.

## 2026-03-24 — feature/code-review-eval-skill

Diff: 3 files, +130 lines (docs/skill only, no Go code)

| Config | Findings | FPs | Verdict | Confidence | Time | Tokens |
|--------|----------|-----|---------|------------|------|--------|
| cursor-composer2 | 6 | 0 | incorrect | 0.82 | 74s | n/a (cursor) |
| codex-5.4 | 3 | 0 | incorrect | 0.95 | ~120s | 0/0 (not reported) |
| codex-5.4-mini | 2 | 0 | incorrect | 0.89 | ~90s | 0/0 (not reported) |

Consensus: branch/PR argument not wired to review commands; token counts unavailable for cursor
Unique to cursor-composer2: incomplete sentence in known-blind-spots.md, origin/main assumption, log merge conflicts, run_in_background tooling-specific, security/logs
Unique to codex-5.4: write-capable defaults can mutate checkout between runs (highest severity finding)
Unique to codex-5.4-mini: config name mismatch (nano vs mini in uncommitted edit)
Blind spots: none of the three caught that eval-runs.log header still references "ground truth" (stale from pre-simplification)

Notes:
- codex-5.4-nano run failed (model does not exist, correct name is gpt-5.4-mini)
- Token counts show 0/0 for all codex runs — reporting issue in codex backend
- Cursor backend never reports tokens (known limitation)
- codex-5.4 found the most architecturally significant issue (benchmark invariant violation)

## 2026-03-24 (round 2) — feature/code-review-eval-skill

Diff: 3 files, +155 lines (after fixing round 1 findings)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| cursor-composer2 | 7 | 0 | correct | 0.84 | 78s |
| codex-5.4 | 3 | 0 | incorrect | 0.96 | ~150s |
| codex-5.4-mini | 2 | 0 | incorrect | 0.93 | ~90s |

Consensus: cursor --force has no read-only guard; branch/PR argument mismatch with cobra.NoArgs
Unique to cursor-composer2: protocol log filename collision, duplicated CLI wiring, custom containsString, run_in_background specificity, stale eval-runs.log commentary
Unique to codex-5.4: base branch hardcoded to origin/main
Unique to codex-5.4-mini: --read-only missing from command template (valid — prose said it but template didn't)

Notes:
- Round 1 findings (model name, blind spots concept, token column) were all fixed
- Cursor-composer2 expanded scope to review Go code (not just skill docs), found 7 findings
- Both codex models stayed focused on skill correctness, found fewer but higher-severity issues
- codex-5.4-mini caught a subtle inconsistency (prose vs template) that others missed

## 2026-03-24 (round 3) — feature/code-review-eval-skill

Diff: 3 files, +170 lines (after fixing round 2 findings)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| cursor-composer2 | 8 | 0 | correct | 0.86 | 82s |
| codex-5.4 | 2 | 0 | incorrect | 0.92 | ~120s |
| codex-5.4-mini | 2 | 0 | incorrect | 0.89 | ~90s |

Consensus: origin/main assumption in merge-base (cursor + codex-5.4)
Unique to codex-5.4: cursor --force worktree isolation needed (high — inherent backend limitation)
Unique to codex-5.4-mini: --read-only only blocks Write tool not shell edits (valid nuance from reviewer.go doc)
Unique to cursor-composer2: log table inconsistency, bazel binary path portability, stale eval note

Notes:
- Round 2 findings (command template flags, argument-hint, cursor-last ordering) were all fixed
- Cursor-composer2 gave "correct" verdict for second consecutive round
- Codex models remain stricter — both still say "incorrect" due to inherent Cursor backend limitation
- The remaining high finding (Cursor can mutate checkout) requires worktree isolation to fully solve
- codex-5.4-mini found the deepest implementation detail (--read-only scope limitation)

## 2026-03-24 (round 4) — feature/code-review-eval-skill

Diff: 6 files, +205 lines (now includes Go code: --read-only default change)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| cursor-composer2 | 4 | 0 | correct | 0.88 | 76s |
| codex-5.4 | 4 | 0 | incorrect | 0.93 | ~130s |
| codex-5.4-mini | 2 | 0 | incorrect | 0.84 | ~90s |

Consensus: missing unit test for ReadOnly:true → ApprovalPolicyOnFailure path
Unique to codex-5.4: yoloswe/swe.go also uses reviewer without ReadOnly (high); cursor --force (high)
Unique to codex-5.4-mini: build-then-checkout ordering in skill (high); --read-only scope limitation
Unique to cursor-composer2: ReadOnly comment misleading (CLI vs struct); eval-runs.log merge conflicts

Notes:
- First round with Go code changes in the diff — all three reviewers engaged more deeply
- codex-5.4 found that swe.go is another caller that doesn't get the new default (real gap)
- cursor-composer2 gave "correct" for 3rd consecutive round, confidence rising (0.84→0.86→0.88)
- codex-5.4-mini found build ordering issue that others missed

## 2026-03-24 (round 5) — feature/code-review-eval-skill

Diff: 7 files, +215 lines (added unit test for ReadOnly approval policy)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| cursor-composer2 | 4 | 0 | correct | 0.88 | 124s |
| codex-5.4 | 4 | 0 | incorrect | 0.93 | ~140s |
| codex-5.4-mini | 3 | 0 | incorrect | 0.89 | ~90s |

Consensus: default backend is cursor which ignores --read-only (both codex models)
Unique to codex-5.4: swe.go reviewer path without ReadOnly; build-then-checkout ordering
Unique to codex-5.4-mini: origin/main hardcoded in skill
Unique to cursor-composer2: noted swe.go as follow-up not bug; CLI flag wiring acknowledged

Notes:
- Findings have stabilized — same themes repeating across rounds 3-5
- cursor-composer2 4th consecutive "correct" verdict, steady at 0.88 confidence
- codex-5.4 continues to never say "correct" — it treats inherent backend limitations as blockers
- The "default backend is cursor so --read-only is moot" is valid but by design (Cursor is the safer review backend for file mutation since it can't do Bash)

## Cross-round patterns (5 rounds)

- codex-5.4: strictest — never said "correct". Finds architectural gaps (swe.go, benchmark isolation). Best for safety-critical reviews.
- cursor-composer2: most thorough — reads Go source deeply, broadest findings. 4 consecutive "correct" verdicts. Best for comprehensive review.
- codex-5.4-mini: sharpest on subtle details (prose vs template, --read-only scope, build ordering). Fewest findings but highest signal-to-noise.

## 2026-04-18 — feature/better-review

Diff: 8 files, +771/-55 lines (code-review JSON envelope + per-run log)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| cursor-composer2 | 8 | 0 | correct | 0.84 | 77s |
| codex-5.4 | 2 | 0 | incorrect | 0.88 | 209s |
| codex-5.4-mini | 2 | 0 | incorrect | 0.90 | 146s |

Consensus (both codex):
- Effective model not reported in envelope (raw `--model` flag used; empty when defaults apply) — `bramble/cmd/codereview/codereview.go:155`, `yoloswe/reviewer/reviewer.go:201`

Unique to codex-5.4: `BuildEnvelope` treats any syntactically valid JSON object as a success (no schema validation of `verdict`/issues) — `yoloswe/reviewer/json_output.go:81-99,134-138`
Unique to codex-5.4-mini: `--json` not guaranteed on pre-run failure paths (setup/validation/start errors bypass envelope) — `bramble/cmd/codereview/codereview.go:71,128`
Unique to cursor-composer2: global `slog.SetDefault` never restored on cleanup; unused `Config.JSONOutput`/`SkipTestExecution` fields; dual `code-review` CLI drift (`yoloswe/cmd/code-review/main.go`); duplicated "Review Result" stderr block; JSON error-path ignores `PrintJSONResult` return; `maxSeverity` only handles 4 labels; tool-input summaries logged at debug (potential log leakage); `PrintResultSummary` now prints to both stderr and stdout

Notes:
- Cursor-composer2 broadest surface again; useful maintainability/security observations (slog restore, log leakage) no codex model raised
- Both codex models converged on effective-model-reporting gap that cursor did not flag
- codex-5.4 caught a protocol-level correctness bug (empty JSON body → status ok); codex-5.4-mini caught the early-failure envelope gap
- cursor's "correct" verdict vs codex's "incorrect" mirrors the strictness pattern from earlier rounds
- Tokens: all reported 0/0 for codex (known issue); cursor doesn't report

## 2026-04-19 — feature/better-review (post-fix round)

Diff: 10 files, +946/-59 lines (after commit 46171c6 closed JSON envelope contract gaps)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| codex-5.4 | 3 | 0 | incorrect | 0.85 | 273s |
| codex-5.4-mini | 2 | 0 | incorrect | 0.89 | 158s |
| cursor-composer2 | 6 | 0 | correct | 0.84 | 67s |

Consensus:
- `maxSeverity` unknown-label handling doesn't match its comment — `unknownRank=1` ties `low` with strict `>`, so unknown labels can be suppressed by a prior `low`. `bramble/cmd/codereview/codereview.go:185-201`. (codex-5.4-mini + cursor)

Unique to codex-5.4: Cursor `ReadyEvent.Model` never feeds `Reviewer.EffectiveModel()`, so `--backend cursor --json` reports empty/stale model (confirmed against `backend_cursor.go:108-112` and `reviewer.go:297`); `summarizeToolInput` writes truncated tool args to per-run log every run (potential leak of shell commands, paths — confirmed in cursor's own run log); dual entrypoint `yoloswe/cmd/code-review/main.go` lacks `--json`/`--skip-test-execution`/run log.

Unique to codex-5.4-mini: `validateReviewBody` only checks verdict + issue severity/message — doesn't reject `accepted` with non-empty issues, `rejected` with empty issues, missing `file`/`line`, or arbitrary severity labels. `json_output.go:121-133`.

Unique to cursor-composer2: `PrintJSONResult` error discarded on error-envelope path (`codereview.go:150,215`); `Config.JSONOutput` field is set but never read inside reviewer; explicitly re-raises the dual-entrypoint divergence; recapped prior-round findings.

Notes:
- Commit 46171c6 closed the previous round's envelope-contract and effective-model gaps for Codex, but the Cursor path still doesn't update EffectiveModel from ReadyEvent (verified).
- `maxSeverity` bug is real and actionable — two different reviewers, same finding, verified by reading the code.
- codex-5.4 caught the debug-log leakage and confirmed it (cursor's own run produced log lines containing shell command fragments and absolute paths).
- `validateReviewBody` findings are legit design concerns but arguably a "stricter validation" follow-up rather than a correctness bug.
- cursor-composer2 saw the implementation correctness was sound, issued "correct" — consistent with its pattern of accepting inherent-design limitations as acceptable.
- codex-5.4 was 4x slower than cursor for 3 findings vs cursor's 6, but those 3 were higher-signal (model-report bug, log leakage both verified).

## 2026-04-19 (round 2) — feature/better-review (post commit 278b516)

Diff: 13 files, +1185/-64 lines (after fix-eval commit closed prior round's findings)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| codex-5.4 | 2 | 0 | incorrect | 0.86 | 271s |
| codex-5.4-mini | 2 | 0 | incorrect | 0.88 | 137s |
| cursor-composer2 | 8 | 0 | correct | 0.82 | 42s |

Consensus (all three):
- `validateReviewBody` is too lax: only checks verdict membership + issue severity/message presence. Doesn't enforce file/line citation, doesn't reject inconsistent verdict/issues combos, doesn't constrain severity labels. `yoloswe/reviewer/json_output.go:121-133`. (all three reviewers, cursor framed as "intentionally minimal" rather than a bug)

Consensus (codex-5.4 + cursor):
- Dual entrypoint `yoloswe/cmd/code-review/main.go` still lacks `--json`/`--skip-test-execution`/run log/envelope path. Maintainability/DX trap. (raised in 4 of last 4 rounds — never fixed)

Unique to codex-5.4-mini: `cwd` key missing from `sensitiveToolInputKeys` despite Codex shell tool start emitting `cwd` (verified at `agent-cli-wrapper/codex/events.go:233`). Per-run logs persist full workspace paths. `yoloswe/reviewer/backend.go:221-233`. **High signal — verified, not raised by other configs.**

Unique to cursor-composer2: `emitEarlyFailure` discards `PrintJSONResult` return value (`codereview.go:217-224`); `Config.JSONOutput` field unused inside reviewer; `--json` review-failure path: print error only logged, original review error returned (caller can't tell stdout was incomplete); `PrintResultSummary` always-on stdout duplicate when interactive; run log captures user-supplied `goal` verbatim; tool-input redaction uses allowlist of sensitive keys but non-listed keys still pass through `%v`.

Notes:
- Commit 278b516 closed last round's `maxSeverity` and effective-model bugs (verified — cursor reports model in stderr now).
- All three converged on `validateReviewBody` strictness — same finding, three rounds running. This is now a stable "agreed gap" rather than a fresh discovery.
- codex-5.4-mini's `cwd` redaction finding is the round's standout: concrete, verifiable, novel, security-relevant. Other configs missed it.
- cursor-composer2 was fastest by 3-6x and broadest (8 findings); no false positives but most are minor/style.
- cursor was the only one to grade "correct" again — same pattern as prior rounds (cursor accepts intentional-design choices, codex flags them).
- Token counts still 0/0 for codex (known reporting bug), n/a for cursor.

## 2026-04-19 (round 3) — feature/better-review (post commit 830ebbf)

Diff: 14 files, +1306/-67 lines (after fix-eval round 2 closed cwd redaction + tightened validateReviewBody)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| codex-5.4 | 2 | 0 | incorrect | 0.89 | 251s |
| codex-5.4-mini | 3 | 1 | incorrect | 0.93 | 197s |
| cursor-composer2 | 6 | 0 | correct | 0.87 | 52s |

Consensus (all three):
- **`cwd` redaction is incomplete** — fix only added `cwd` to `sensitiveToolInputKeys` (per-tool-call summaries), but the run-start log line at `bramble/cmd/codereview/codereview.go:89-100` still emits `"cwd", workDir` directly via slog, so per-run logs continue to persist the absolute workspace path. Real bug — verified by reading the code.

Consensus (codex-5.4 + cursor):
- Dual entrypoint `yoloswe/cmd/code-review/main.go` still bypasses --json/run log/envelope. **5th round in a row** — never fixed.

Unique to codex-5.4-mini: protocol-log filename only has second-level precision (`reviewer.go:336-348` + `codex/process.go:86-88` opens with os.Create, two runs in same second collide).

Unique to cursor-composer2: `env.Error` loses parse/schema detail when both ErrorMessage and parseErr/schemaErr trigger (cosmetic — only first set wins); severity vocabulary duplicated between `validSeverities` and `maxSeverity` rank table; no test for `BuildPromptWithOptions(skipTestExecution=true)` suffix; eval-runs.log churn risk.

False positive — codex-5.4-mini:
- "High: `agent-cli-wrapper/claude/turn.go:81` retry detector regression". **FP — those files are NOT in this branch's diff** (`git diff origin/main...HEAD --stat` confirms only code-review files touched). The model hallucinated a regression in unrelated code, weighted it "high", and counted it in the verdict. This is the first verified FP across all rounds. Lowers signal of codex-5.4-mini's verdict for this round.

Notes:
- The cwd fix in commit 830ebbf was incomplete: per-tool-call args are redacted now, but the per-run startup log line still leaks the path. Same finding raised by all three reviewers — high consensus → real.
- codex-5.4-mini broadened scope (3 findings vs 2 in last 3 rounds) but regressed on signal: 1 of 3 findings is hallucinated. Time also nearly doubled (137s → 197s).
- cursor-composer2 stayed broad and useful (6 findings, no FPs); held "correct" verdict (its 7th out of last 8 rounds).
- codex-5.4 was the steadiest: 2 findings, both real, both verifiable.
- The validateReviewBody fix landed cleanly — no reviewer raised contract-strictness as an issue this round, vs being the consensus finding for 3 prior rounds.
- Recurring blind-spot pattern: standalone `yoloswe/cmd/code-review/main.go` flagged 5 rounds running, never fixed. Worth deciding: delete the binary, or sync it with the new contract.

## 2026-04-19 (round 4) — feature/better-review (post commit 322b991)

Diff: 14 files, +1370/-69 lines (after run-start log redaction landed)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| codex-5.4 | 2 | 0 | incorrect | 0.85 | 288s |
| codex-5.4-mini | 1 | 0 | incorrect | 0.93 | 213s |
| cursor-composer2 | 6 | 0 | correct | 0.84 | 52s |

Verified fixes from commit 322b991:
- All three reviewers stopped flagging the cwd-in-run-start leak. Cursor explicitly noted: "Run-start logging no longer dumps raw `cwd` or goal text". Fix landed cleanly.

New consensus (codex-5.4 + cursor): Dual entrypoint `yoloswe/cmd/code-review/main.go` still bypasses --json/runlog/envelope. **6th round in a row** — never fixed.

Unique to codex-5.4-mini: **`pattern` and `globPattern` keys missing from `sensitiveToolInputKeys`** despite Cursor's `grepToolCall` and `globToolCall` emitting them as inputs (verified at `backend_cursor.go:137-149`). Per-run logs persist regex patterns and glob expressions verbatim. **High signal — verified, novel, not raised by other configs.** This is codex-5.4-mini's second consecutive round catching a real, novel redaction-allowlist gap (round 3: cwd; round 4: pattern/globPattern).

Unique to codex-5.4: missing `line` not enforced in `validateReviewBody` despite the prompt schema example showing `"line": 42`. Borderline — file pin alone is still actionable, but pin precision degraded. Severity arguable (codex-5.4 says high; file-level findings can legitimately omit line). Treat as a "tighter contract" follow-up, not a blocker.

Unique to cursor-composer2: `emitEarlyFailure` still discards `PrintJSONResult` error (3rd round flagging this — minor but never fixed); severity vocabulary duplicated between `validSeverities` and `maxSeverity` rank table; no test for `BuildPromptWithOptions(skipTestExecution=true)`; commit message references missing `pr-polish/SKILL.md`; eval-runs.log churn risk.

Notes:
- Round-3's hallucinated codex-5.4-mini finding (turn.go retry) did NOT recur. Codex-5.4-mini stayed in scope and found exactly 1 high-signal real bug. Confidence held at 0.93.
- All redaction-allowlist additions in this branch (cwd, plus the existing list) were caught one round late by codex-5.4-mini. This is now an established pattern: codex-5.4-mini specializes in finding allowlist boundary gaps. Worth treating as a "redaction completeness" reviewer.
- cursor-composer2 reissued its 4th "correct" verdict in 4 rounds; the codex models continue flagging "incorrect" on inherent design concerns.
- The `--json early failure error discarded` is now a 3-round pattern raised by cursor; deserves a follow-up commit even though it's minor.

## 2026-04-19 (round 5) — feature/better-review (post commit 6f15371)

Diff: 14 files, +1416/-72 lines (after redaction expansion + line requirement + early-failure error surfacing)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| codex-5.4 | 3 | 0 | incorrect | 0.76 | 310s |
| codex-5.4-mini | 0 | 0 | **correct** | 0.91 | 202s |
| cursor-composer2 | 6 | 0 | correct | 0.86 | 83s |

Verified fixes from commit 6f15371:
- All three reviewers stopped flagging the cwd, validateReviewBody, and pattern/globPattern findings. cursor explicitly noted: "Tool input summarization redacts paths, commands, edit payloads, and Cursor search-related keys (`pattern`, `globPattern`)".
- `validateReviewBody` line requirement is no longer raised by anyone — even codex-5.4 dropped it from this round's findings.

**Codex-5.4-mini issued first "patch is correct" verdict in 5 rounds** with 0 findings, 0.91 confidence. Notably ran `bazel build //...` AND `bazel test //... --test_timeout=60` (full repo) to validate before signing off — broader than usual.

**New finding from codex-5.4 (real, introduced by my fix):**
- `slog.Error` calls in `emitEarlyFailure` and the review-failure path don't actually surface to stderr after `SetupRunLog` runs. `SetupRunLog` does `slog.SetDefault(base)` where base writes ONLY to the log file (`runlog.go:41-48`). Verified: my "fix" for the print-error-discarded issue was incomplete — the error gets logged to disk but not stderr where the operator would see it. **Self-inflicted regression caught by codex-5.4.** Severity: medium (functional gap, not a security/correctness break).

Other findings from codex-5.4: validateReviewBody doesn't require `summary` field (borderline — schema marks it as advisory); dual entrypoint (7th round running).

Unique to cursor-composer2: `Config.JSONOutput` and `Config.SkipTestExecution` fields are populated but never read inside reviewer (3rd round flagging this — minor); severity vocabulary duplicated; eval-runs.log churn risk.

Notes:
- **First time codex-5.4-mini agreed with cursor on "correct"**. Both verdicts are now "correct" except codex-5.4 (which holds out on the slog-stderr regression).
- The slog-redirect-to-file finding is a textbook case where adding code to address a reviewer concern introduces a new bug because the surrounding state (SetupRunLog) wasn't traced. Worth a follow-up commit: in `emitEarlyFailure` and `runCodeReview` failure path, write the error directly to `os.Stderr` (or a separate stderr handler) instead of trusting the redirected slog.
- codex-5.4 was the slowest run yet (310s); confidence dropped to 0.76 — its lowest in this branch.
- cursor stayed steady at ~80s, broad findings, "correct" verdict (5th in last 5 rounds).
- Pattern across rounds: codex-5.4 catches the most subtle/architectural issues; codex-5.4-mini catches surface-level redaction gaps quickly then declares done; cursor finds the most variety but accepts intentional design as correct.

## 2026-04-19 (round 6) — feature/better-review (post commit db67e16)

Diff: 14 files, +1463/-72 lines (after stderr fix for envelope-print errors)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| codex-5.4 | 3 | 0 | incorrect | 0.79 | 306s |
| codex-5.4-mini | 2 | 0 | incorrect | 0.81 | 198s |
| cursor-composer2 | 4 | 0 | **correct** | 0.90 | 112s |

Verified fixes from commit db67e16:
- All three reviewers stopped flagging the slog-to-disk-only regression. cursor explicitly noted: "`reportEnvelopePrintError` writing to stderr when slog is file-bound" as a strength. Fix landed cleanly.
- Cursor confidence rose from 0.86 → 0.90 (highest of any round on this branch).

New finding from codex-5.4 (real, novel — 1st time raised):
- **`redactPath` preserves basename which IS the branch/worktree name.** Verified: `basename $(pwd)` = `better-review` (the feature branch). In standard worktree layout (`/home/user/worktrees/<repo>/<branch>`), the branch name (often a ticket ID or customer identifier) ends up in every per-run log. The fix from round 4 (commit 322b991) is partial — it dropped home-directory layout but kept branch identifiers. **Real but minor; severity arguable.** Could redact further to `<redacted:N>` only, or document that basename is intentionally kept for run disambiguation.

Other findings from codex-5.4: dual entrypoint (8th round running); CLI test coverage gap (only helpers tested, runCodeReview unverified end-to-end).

codex-5.4-mini findings:
- JSON contract shape concerns (single `line` int vs prompt's "line range"; `summary` not required by validateReviewBody). Both borderline — `Line int` matches the prompt schema example exactly, and `summary` is advisory in the prompt. **Treat as nice-to-haves, not bugs.**
- runCodeReview not directly tested (consensus with codex-5.4 above).

Unique to cursor-composer2: severity vocabulary duplicated (5th round flagging — accepted as design); SetupRunLog modifies global slog (acknowledged as CLI-acceptable side effect); eval-runs.log churn risk.

Notes:
- Round 5's regression (slog-to-disk-only) caught and fixed in one cycle. Both call sites (emitEarlyFailure + review-failure path) now go through reportEnvelopePrintError which writes to stderr first then slog.
- codex-5.4-mini reverted from "correct" to "incorrect" — it noticed the runCodeReview test gap which it had missed in round 5. This is actually an improvement in attention.
- Both codex models converged on **CLI integration test gap** as the remaining real concern. Worth fixing — would be the cleanest way to catch future regressions like the slog-routing one before they ship.
- cursor's 0.90 confidence is the highest signal of branch readiness yet. With its broad coverage and "correct" verdict, the branch is in solid shape for merge.
- The dual-entrypoint finding has been raised in 8 of 8 rounds on this branch by codex-5.4 (and most rounds by cursor). Until someone decides to delete or sync `yoloswe/cmd/code-review/main.go`, every reviewer will keep raising it. Consider closing the loop with an explicit decision commit.

## 2026-04-20 (round 7) — feature/bramble-stream-envelope-guard

Diff: 7 files, +621/-19 lines (progress stream + always-emit envelope guard)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| cursor-composer2 | 3 | 0 | rejected | — | 77s |
| codex-5.4-mini | 1 | 0 | rejected | — | 169s |
| codex-5.4 | 1 | 0 | rejected | — | 220s |

**Note on codex-5.4 observation:** For the first ~200s of the run, the stdout file showed only the `session-started` progress event and the log had only 2 lines (no exit entry). The process eventually completed at 220s. This delayed-output pattern is expected with `run_in_background` + `tee` — the task runner captures stdout when the pipe closes, so intermediate output isn't visible until the process exits.

Cursor findings:
1. **high**: `emitEnvelope` marks `envelopeWritten` only after `PrintJSONResult` returns nil, but `PrintJSONResult` does two separate `Write` calls. If the first write succeeds and the second fails, the JSON object is already on stdout but `envelopeWritten` stays false — `finalizeEnvelope` then emits a second envelope, breaking the single-envelope guarantee. Fix: single `Write` for payload+newline in `PrintJSONResult`.
2. **medium**: `OnError` copies `StreamErrorContext` into `ProgressEvent.Detail` without sanitization, potentially leaking host-specific paths via `--json` progress stream to automation consumers. The ProgressEvent doc claims Detail avoids full paths.
3. **medium**: `Cmd.Long` still describes `--json` as emitting "a stable JSON object on stdout" — help text is now misleading since NDJSON progress lines precede the envelope.

codex-5.4-mini findings:
1. **high**: `finalizeEnvelope` fallback path (line 306) emits an error envelope when no JSON was written, but never sets `*a.retErr`. The command can exit 0 while stdout reports failure. Fix: set `*a.retErr` before emitting the synthesized error envelope.

codex-5.4 findings:
1. **medium**: `OnToolStart` in `backend.go` (line ~213) stores the rendered tool label in `ProgressEvent.Tool`. On Cursor, that label includes per-call path/command context (e.g. `read .../file.go`), so the coalescer keys each invocation separately — bursts never coalesce by tool type, defeating the event-budget protection. Fix: emit stable tool identifier (`read`, `shell`) in `Tool`; put optional context in a sanitized `Detail` field.

Consensus: none — each reviewer found a distinct concern.

Unique to cursor: two-write atomicity in `PrintJSONResult` (high); `OnError` Detail sanitization; `Cmd.Long` help text staleness.

Unique to codex-5.4-mini: `finalizeEnvelope` fallback returns nil on guard-emit (exit-0 on synthesized failure).

Unique to codex-5.4: coalescing key uses rendered label (path-enriched) instead of stable tool type — coalescing never fires for Cursor in practice.

Notes:
- This is the first round on this branch. Three reviewers found three distinct real bugs.
- The `NDJSONProgressEmitter` data race (score 100 from /review agents) was NOT caught by any of the three reviewers — all focused on `finalizeEnvelope` and coalescing instead.
- codex-5.4-mini's finding (exit-0 on guard-emit) is the most actionable for automation consumers.
- codex-5.4's coalescing-key finding is novel: `summarizeToolInput` labels include path context, so each `read` of a different file is a unique key — the 10s window is effectively dead for Cursor.
- cursor's two-write atomicity concern is real but lower probability than the mutex race and exit-code issue.
- Times: cursor 77s (3 findings), mini 169s (1 finding), codex-5.4 220s (1 finding).

## 2026-04-20 (round 8) — feature/bramble-stream-envelope-guard (same commit, second run)

Diff: 7 files, +621/-19 lines (same as round 7 — no new commits)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| cursor-composer2 | 2 | 0 | **accepted** | — | 55s |
| codex-5.4 | 2 | 0 | rejected | — | 195s |
| codex-5.4-mini | 2 | 0 | rejected | — | 202s |

Cursor findings (medium):
1. `Cmd.Long` still says `--json` emits a single JSON object — misleading since NDJSON progress precedes envelope (9th+ round raising this)
2. `PrintJSONResult` does two `Write` calls — partial write leaves `envelopeWritten=false`, guard emits second envelope

codex-5.4 findings:
1. **high**: `emitEnvelope` swallows `PrintJSONResult` failures on the success path and `retErr` stays nil — if stdout is broken/pipe closed, command exits 0 with no valid envelope on stdout. Regresses the previous behavior where write errors surfaced as command errors.
2. **medium**: help text stale (same as cursor finding 1)

codex-5.4-mini findings:
1. **high**: Same as codex-5.4 high — write failure on success path keeps `retErr` nil, `finalizeEnvelope` returns success. Exit 0 with no valid envelope.
2. **medium**: `ProgressKindVerdict` emitted unconditionally even when `BuildEnvelope` classifies run as `StatusError` (malformed reviewer JSON, non-success backend). Produces a normal completion marker (`issue_count`) for a failed review.

Consensus (codex-5.4 + codex-5.4-mini): `emitEnvelope` write failure → exit 0 with no envelope. This is the highest-signal finding of round 8 and sharpens round 7's `retErr` concern — the root is that `emitEnvelope`'s error goes to stderr only, not into `retErr`.

Consensus (cursor + codex-5.4): help text stale (2nd round in a row).

Unique to codex-5.4-mini: `ProgressKindVerdict` emitted on `StatusError` runs — false success signal for error exits.

Notes:
- Cursor flipped to "accepted" this round (was "rejected" round 7). It accepted despite raising the two-write atomicity concern again — it now considers the overall design correct, gaps are documentation/defensive only.
- Both codex models independently zeroed in on the write-failure/exit-0 gap. This is the most actionable finding: fix `emitEnvelope` to return an error and propagate into `retErr` on the success path.
- codex-5.4-mini's `ProgressKindVerdict` finding is new and real — the verdict event should only fire on `StatusOK` or should carry the error status, not always fire.
- The NDJSONProgressEmitter mutex race (score 100 from /review) still not caught by any reviewer in round 8 either. Two rounds, three reviewers each, zero hits on the race.
- The help text finding (raised every round by all reviewers) is now clearly the #1 low-effort fix: update `Cmd.Long` to describe NDJSON stream.

## 2026-04-20 (round 9) — feature/bramble-stream-envelope-guard (same commit, third run)

Diff: 7 files, +621/-19 lines (no new commits since rounds 7-8)

| Config | Findings | FPs | Verdict | Time |
|--------|----------|-----|---------|------|
| cursor-composer2 | 3 | 0 | **accepted** | 59s |
| codex-5.4-mini | 2 | 0 | rejected | 122s |
| codex-5.4 | 3 | 0 | rejected | 284s |

Cursor findings:
1. **medium**: `Cmd.Long` help text stale — describes `--json` as single envelope, now NDJSON (every round)
2. **medium**: `emitEnvelope` write failure leaves `envelopeWritten=false`/`retErr=nil`; `finalizeEnvelope` then emits generic "exited without review" instead of surfacing the real write failure
3. **low**: Comment at `progress.go:63` says structural kinds pass because "interval 0" but they actually pass via switch-case exemption — misleading for maintainers

codex-5.4-mini findings:
1. **high**: Installing progress emitter on `os.Stdout` for `--json` makes the stream emit multiple JSON objects — breaks existing single-envelope contract, callers that unmarshal-once fail on first progress event
2. **medium**: Coalescer `last` map never resets on `ProgressKindTurnComplete`; reused `Reviewer` or multi-turn run suppresses the first tool-use event of the next turn if it arrives inside the previous window

codex-5.4 findings:
1. **high**: `ProgressEvent.Tool` stores the renderer display name (e.g. `read .../file.go`) not a canonical tool kind — leaks path fragments to `--json` stdout, coalescer never dedupes repeated `read`/`shell` across different files (same finding as round 7, now called high severity)
2. **medium**: `ProgressKindVerdict` emitted unconditionally even when `env.Status == "error"` — same as round 8 mini finding, now confirmed by codex-5.4
3. **medium**: help text stale (same as cursor)

Consensus (all 3): `Cmd.Long` help text stale — lowest-effort fix, every reviewer in every round.
Consensus (codex-5.4 + codex-5.4-mini): `ProgressKindVerdict` on error runs — 2nd consecutive round both raise this; real bug.
Consensus (codex-5.4 + codex-5.4-mini, design disagreement with cursor): NDJSON-on-stdout breaks single-envelope contract vs. cursor accepting this as intentional design with stale docs.

Unique to codex-5.4-mini: coalescer `last` map leaks across turns — new finding, not previously raised.
Unique to codex-5.4: Tool field uses rendered display name not canonical kind (3rd round raising this).
Unique to cursor: stale-comment in `progress.go:63` — new low finding.

Notes:
- The NDJSON-vs-single-envelope contract debate is the core design question for this branch: is `--json` intentionally NDJSON now (update docs) or should progress go to stderr to preserve the single-envelope contract? codex-5.4-mini rates it high (breaking change); cursor rates it medium/docs (intentional). This needs an explicit decision.
- The coalescer turn-reset finding (mini) is novel and worth fixing: `Reviewer` is documented as reusable, and the `last` map is never cleared, so a second review session on the same `Reviewer` instance would suppress early events. Easy fix: clear `e.last` on `TurnComplete` or scope by session ID.
- The `ProgressKindVerdict`-on-error finding has now appeared in 3 consecutive reviewer runs across 2 rounds (round 8 mini, round 9 mini+codex-5.4). Strong consensus — fix it.
- codex-5.4 was slowest again (284s vs 122s for mini, 59s for cursor).
- The mutex race in `NDJSONProgressEmitter` (score 100) still not caught by any reviewer — 9 total reviewer runs, 0 hits.

## 2026-04-21 (round 10) — feature/bramble-stream-envelope-guard (post commit 19b02b8)

Diff: 7 files, +652/-23 lines (after fixes: mutex race, ProgressKindVerdict gating, emitEnvelope error propagation, single-Write atomicity, TurnComplete coalescer reset, help text, stale comment)

| Config | Findings | FPs | Verdict | Time |
|--------|----------|-----|---------|------|
| cursor-composer2 | 3 | 0 | **accepted** | 58s |
| codex-5.4 | 1 | 0 | rejected | 199s |
| codex-5.4-mini | 2 | 0 | rejected | 193s |

Verified fixes (none of the three raised these): mutex race, ProgressKindVerdict on StatusError, stale help text (Cmd.Long now describes NDJSON stream), stale progress.go comment, TurnComplete coalescer reset (all three did not re-raise any of these).

cursor-composer2 findings (all medium):
1. If `PrintJSONResult` fails after a partial write, `envelopeWritten` stays false and `finalizeEnvelope` emits a second error envelope — truncated first line + second JSON object breaks line-by-line consumers (`codereview.go:84`)
2. Progress write errors silently discarded — broken stdout or serialization failure causes silent loss of progress lines with no signal to automation (`progress.go:123`)
3. No integration-style test for full `--json` stdout ordering: multiple progress lines + exactly one terminal envelope (`codereview_test.go:121`)

codex-5.4 findings:
1. **high**: `ProgressKindVerdict` emitted at lines 218-235 before `emitEnvelope(env)` call. If `emitEnvelope` fails, the deferred guard synthesizes an error envelope — consumer sees a success `verdict` event followed by an error terminal result, reintroducing the false-success signal on stdout I/O failure (`codereview.go:218`)

codex-5.4-mini findings:
1. **high**: `emitEnvelope` sets `retErr` but subsequent `return emitEarlyFailure(...)` / `return nil` paths can overwrite it — stdout write failure not reliably surfaced on all exit paths (`codereview.go:83`)
2. **medium**: Coalescer only resets on `TurnComplete`, not `session-started` or `error` — aborted-turn reuse still suppresses first tool-use of next session (`progress.go:137`)

Consensus: none across all three. Each reviewer found distinct remaining concerns.

Notes:
- The mutex race, ProgressKindVerdict gating, and single-Write atomicity fixes all landed cleanly — no reviewer re-raised any of them.
- codex-5.4 and codex-5.4-mini both zeroed in on the same failure mode (verdict-before-write / retErr-overwritten) from different angles. The root concern is identical: a stdout write failure on the happy path can still produce a misleading success signal. The codex-5.4-mini framing (return-path overwrites) is the more precise characterization.
- codex-5.4-mini's second finding (reset on session-started) is novel and was flagged in round 10's preview — it's a legitimate edge case for aborted sessions but lower-priority than the write-failure issue.
- cursor accepted the overall design again (3 medium findings, no blockers). Its "double envelope on partial write" finding is a residual atomicity concern — the single-Write fix in this commit helps but doesn't fully eliminate the window if the kernel partial-writes the combined buffer.
- The mutex race (score 100 from /review) was never caught in 10 rounds across 3 reviewers = 10 total runs. Now fixed, no regression observed.
- **This is the final eval round per user instruction.**

## 2026-04-21 — feature/bramble-stream-envelope-guard

Diff: 7 files, +346/-114 lines (replace --json with --envelope-file, remove NDJSON progress machinery)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| cursor-composer2 | 6 | 0 | rejected | high | 154s |
| codex-5.4-mini | 2 | 0 | rejected | high | 275s |

Consensus (both): 
- [high] return nil on success path silently discards envelope write errors from emitEnvelope (codereview.go:209-210)
- [medium] no end-to-end test for --envelope-file / stdout-split contract

Unique to cursor:
- [medium] OnError omits actual err value from stdout line (backend.go:231)
- [low] ResultEnvelope doc comment still references removed --json flag (json_output.go:70)
- [low] captureStdStreams test comment still says "--json paths" (codereview_test.go:93)
- [low] TestFinalizeEnvelope_PanicAlwaysEmitsAndRepanics largely duplicates TestFinalizeEnvelope_PanicEmitsEnvelopeThenRepanics

Unique to codex-5.4-mini: none beyond consensus

Disagreements: none — codex found a strict subset of cursor's findings

## 2026-04-21 (round 12) — feature/bramble-stream-envelope-guard (post commit 4bd67cc)

Diff: 10 files, +539/-155 lines (replace fmt.Fprintf+slog duplication with dual-write tee handler; address round 11 correctness gaps)

| Config | Findings | FPs | Verdict | Confidence | Time |
|--------|----------|-----|---------|------------|------|
| cursor-composer2 | 2 | 0 | rejected | high | 173s |
| codex-5.4-mini | 3 | 0 | rejected | high | 177s |

Envelope guard fired: No — both configs received proper envelopes via --envelope-file.

Consensus (both):
- [high] `emitEnvelope` write failure on success path: `return nil` at the end of the success path silently discards the error from `PrintJSONResult` (codereview.go:205 area). Backend failures and write errors exit 0 while stdout has no valid envelope. This is the **same finding as round 11** — not yet fixed.

Unique to cursor:
- [high] Double-envelope on partial write: if `emitEnvelope` fails after a partial write (open succeeds, write fails), `envelopeWritten` stays false and `finalizeEnvelope` emits a second error envelope to the same sink — truncated first JSON + second object corrupts line-by-line consumers (codereview.go:288). Also a **recurring finding** from rounds 8-11.

Unique to codex-5.4-mini:
- [medium] `os.Create` for `--envelope-file` uses default umask permissions instead of explicit `0600` — envelope can contain repo paths and review findings, may be world-readable by default (codereview.go:253).
- [medium] `--envelope-file` open failure: if the file cannot be opened, `emitEnvelope` logs and returns without writing anywhere; the deferred `finalizeEnvelope` retries the same broken path — callers lose the machine-readable result entirely (codereview.go:83).

Disagreements: none — both flagged the same root issue (exit-0 on write failure) but cursor added the double-envelope consequence and codex-5.4-mini added the file-permission and open-failure concerns.

Notes:
- The consensus exit-0-on-write-failure finding has now appeared in rounds 8, 9, 10, 11, and 12 (5 consecutive rounds). The round 11 fix addressed some paths but left the success-path `return nil` intact. This is now the clearest remaining blocker.
- codex-5.4-mini's file permission finding (0600 vs umask) is new — appeared first this round despite the `os.Create` being present since the --envelope-file feature landed. High signal novel finding.
- codex-5.4-mini's open-failure fallback finding is also new and complements the permission concern — both are about the file-opening path.
- cursor's double-envelope concern (rounds 8+) remains open. A single atomic write would fix it, but the root cause is that `envelopeWritten` isn't set on partial-write failure.
- Both configs ran in ~175s — unusually tight timing (within 4s of each other). No envelope guard activations.
- The dual-entrypoint `yoloswe/cmd/code-review/main.go` was NOT raised this round by either reviewer. May have been addressed or deprioritized.
