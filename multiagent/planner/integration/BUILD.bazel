# gazelle:ignore
load("@rules_go//go:def.bzl", "go_test")

go_test(
    name = "integration_test",
    srcs = ["planner_test.go"],
    gotags = ["integration"],
    tags = [
        "integration",
        "manual",
    ],
    deps = [
        "//multiagent/agent",
        "//multiagent/checkpoint",
        "//multiagent/planner",
        "//multiagent/progress",
        "//multiagent/protocol",
    ],
)
